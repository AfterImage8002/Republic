add_namespace = FRA_event

country_event = {
    id = FRA_event.1
    title = FRA_event.1.t 
    desc = FRA_event.1.d 

    fire_only_once = yes
    is_triggered_only = yes

    option = {
        name = FRA_event.1.a
        RFR = { transfer_state = 16}
    }
}